import { ControlBase, ControlName } from './base';
export declare namespace Paragraph {
    type Props = {
        id: string;
        text: string;
    };
    type Config = ControlBase<ControlName.PARAGRAPH> & Props;
    const create: (props: Props) => Config;
}
