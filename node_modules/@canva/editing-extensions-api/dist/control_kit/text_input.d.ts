import { ControlBase, ControlName } from './base';
export declare namespace TextInput {
    interface InputType {
        id: string;
        placeholder?: string;
        label?: string;
        name?: string;
        disabled?: boolean;
    }
    interface TextType {
        inputType?: 'text';
        value?: string;
        maxLength?: number;
    }
    interface NumberType {
        inputType?: 'number';
        value?: number;
    }
    type Props = InputType & (TextType | NumberType);
    type Config = ControlBase<ControlName.TEXT_INPUT> & Props;
    type Message = {
        type: 'change';
        value: Props['value'];
    };
    const create: (props: Props) => Config;
}
