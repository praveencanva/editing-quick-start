import { ControlBase, ControlName } from './base';
export declare namespace Checkbox {
    type Props = {
        id: string;
        label?: string;
        disabled?: boolean;
        checked?: boolean;
    };
    type Config = ControlBase<ControlName.CHECKBOX> & Props;
    type Message = {
        type: 'change';
        checked: boolean;
    };
    const create: (props: Props) => Config;
}
