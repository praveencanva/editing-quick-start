import { ControlBase, ControlName } from './base';
export declare namespace Button {
    type Props = {
        id: string;
        label: string;
    };
    type Config = ControlBase<ControlName.BUTTON> & Props;
    type Message = {
        type: 'click';
    };
    const create: (props: Props) => Config;
}
