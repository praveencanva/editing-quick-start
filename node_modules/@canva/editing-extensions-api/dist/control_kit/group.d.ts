import { ControlBase, ControlName } from './base';
import { ControlConfig } from './control_kit';
export declare namespace Group {
    type Props = {
        id: string;
        label: string;
        children: ControlConfig[];
    };
    type Config = ControlBase<ControlName.GROUP> & Props;
    const create: (props: Props) => Config;
}
