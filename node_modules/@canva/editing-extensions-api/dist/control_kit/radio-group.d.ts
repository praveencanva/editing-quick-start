import { ControlBase, ControlName } from './base';
export declare namespace RadioGroup {
    type Button = {
        value: string;
        label: string;
        disabled?: boolean;
    };
    type Props = {
        id: string;
        buttons: Button[];
        value?: string;
    };
    type Config = ControlBase<ControlName.RADIO_GROUP> & Props;
    type Message = {
        type: 'setValue';
        value: string;
    };
    const create: (props: Props) => Config;
}
