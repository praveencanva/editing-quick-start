import { ControlBase, ControlName } from './base';
export declare namespace Slider {
    type Props = {
        id: string;
        label: string;
        value: number;
        min: number;
        max: number;
        step: number;
    };
    type Config = ControlBase<ControlName.SLIDER> & Props;
    type Message = {
        type: 'setValue';
        value: number;
    };
    const create: (props: Props) => Config;
}
