import { ControlBase, ControlName } from './base';
export declare type ThumbnailId = string & {
    __thumbnailId: never;
};
export declare namespace ThumbnailId {
    const create: (id: string) => ThumbnailId;
}
export declare namespace ThumbnailList {
    type Item = {
        thumbnailId: string;
        label: string;
        defaultSrc: string;
    };
    type Props = {
        id: string;
        items: Item[];
        selectedThumbnailId?: string;
        showNone?: boolean;
        showCustom?: boolean;
    };
    type Config = ControlBase<ControlName.THUMBNAIL_LIST> & Props;
    type Message = {
        type: 'selected';
        /** id of the thumbnail selected */
        thumbnailId: string;
    };
    const create: (props: Props) => Config;
}
