import { BusComms } from './bus_comms';
import { Message } from './canva_api';
/**
 * @internal
 */
export declare class IFrameClientBusComms implements BusComms<Message.FromClient, Message.FromHost> {
    private readonly port;
    private static MAILBOX_BUFFER_SIZE;
    private static MESSAGING_TIMEOUT;
    private readonly envelopeMinter;
    private readonly mailbox;
    private readonly messageHandler;
    constructor(port: MessagePort);
    onMessage(handler: Message.Handler<Message.FromHost, Message.FromClient>): void;
    castMessage(msg: Message.FromClient): void;
    sendMessage<T extends Message.FromHost>(msg: Message.FromClient, timeout?: number): Promise<T>;
    private respondTo;
    private onPortMessage;
}
